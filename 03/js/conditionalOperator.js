// let allowedToDance;
let userAge = +prompt('How old are you?', '');

if (age > 18) {
  allowedToDance = true;
} else {
  allowedToDance = false;
}

alert(allowedToDance);

let allowedToDance = (age > 18) ? true : false;
